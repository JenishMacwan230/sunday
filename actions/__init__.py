"""
The 'actions' package is SUNDAY's "hands" - one file per real-world
capability (apps, browser, files, typing), plus action_executor.py which
routes the LLM's JSON decision to the correct one.
"""
