# Core voice pipeline
faster-whisper
sounddevice
numpy

# Local LLM connection (talks to Ollama, which you install separately)
ollama

# Voice feedback
pyttsx3

# Keyboard dictation
keyboard

# Floating icon + system tray UI
PySide6

# Optional but recommended on Windows for more reliable app/window control.
# Uncomment the line below if you plan to extend SUNDAY with window-focus
# features later (not required for the base version).
# pywin32
